repos:
  - repo: https://github.com/KartikHalkunde/VH26-CodeBlooded
    # Immutable release tag: reproducible installs; never point this at a branch.
    rev: v0.1.0
    hooks:
      - id: leakguard-strict
